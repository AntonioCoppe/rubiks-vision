export const solveCube = async (state54: string): Promise<string[]> => {
  // TODO: call solver implementation, return moves as ["R","U","R'","U'"]
  // For now, mock:
  return ['R', 'U', "R'", "U'", "F'", 'U', 'F'];
};

